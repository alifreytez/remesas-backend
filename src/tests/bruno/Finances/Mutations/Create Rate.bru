meta {
  name: Create Rate
  type: http
  seq: 73
}

post {
  url: {{baseUrl}}/finances/rates
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
body:json {
  {}
}
