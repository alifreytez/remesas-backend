meta {
  name: Delete Client
  type: http
  seq: 62
}

delete {
  url: {{baseUrl}}/clients/1
  body: none
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
