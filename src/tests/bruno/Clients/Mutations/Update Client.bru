meta {
  name: Update Client
  type: http
  seq: 61
}

patch {
  url: {{baseUrl}}/clients/1
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
body:json {
  {}
}
