meta {
  name: Update Remittance Status
  type: http
  seq: 71
}

patch {
  url: {{baseUrl}}/remittances/1/status
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
body:json {
  {}
}
