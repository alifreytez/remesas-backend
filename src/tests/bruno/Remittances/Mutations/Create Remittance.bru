meta {
  name: Create Remittance
  type: http
  seq: 70
}

post {
  url: {{baseUrl}}/remittances
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
body:json {
  {}
}
