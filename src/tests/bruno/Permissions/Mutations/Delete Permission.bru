meta {
  name: Delete Permission
  type: http
  seq: 47
}

delete {
  url: {{baseUrl}}/permissions/1
  body: none
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
