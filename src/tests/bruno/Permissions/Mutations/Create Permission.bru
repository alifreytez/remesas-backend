meta {
  name: Create Permission
  type: http
  seq: 45
}

post {
  url: {{baseUrl}}/permissions
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
body:json {
  {}
}
