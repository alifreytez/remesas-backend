meta {
  name: Delete
  type: http
  seq: 56
}

delete {
  url: {{baseUrl}}/catalogs/some-catalog/1
  body: none
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
