meta {
  name: Get Metadata
  type: http
  seq: 50
}

get {
  url: {{baseUrl}}/catalogs/some-catalog/metadata
  body: none
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
