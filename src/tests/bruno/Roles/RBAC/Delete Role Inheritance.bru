meta {
  name: Delete Role Inheritance
  type: http
  seq: 40
}

delete {
  url: {{baseUrl}}/roles/1/inheritances/2
  body: none
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
