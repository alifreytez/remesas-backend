meta {
  name: Create Role
  type: http
  seq: 31
}

post {
  url: {{baseUrl}}/roles
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
body:json {
  {}
}
