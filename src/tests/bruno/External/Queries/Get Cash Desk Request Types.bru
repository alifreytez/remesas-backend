meta {
  name: Get Cash Desk Request Types
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/external/cash-desk-request-types?available=true
  body: none
  auth: bearer
}

query {
  available: true
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}

script:pre-request {
  const baseUrl = bru.getEnvVar('baseUrl') || 'http://127.0.0.1:4000/api/v1';
  bru.setVar('baseUrl', baseUrl);
}
