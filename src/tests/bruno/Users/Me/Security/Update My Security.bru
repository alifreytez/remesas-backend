meta {
  name: Update My Security
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/users/me/security
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "claveActual": "dinformatica*2026**",
    "usuario": "28019240",
    "emailGestion": "pastoralirio6589@gmail.com"
    //"clave": "dinformatica*2026**"
  }
}

tests {
  test("Should successfully update user security credentials", function() {
    const resBody = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(resBody.success).to.equal(true);
  });
}
