meta {
  name: Create My Contact
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/users/me/contacts
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
body:json {
  {}
}
