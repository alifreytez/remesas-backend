meta {
  name: Create User
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
body:json {
  {}
}
