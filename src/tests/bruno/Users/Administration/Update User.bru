meta {
  name: Update User
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/users/1
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
body:json {
  {}
}
