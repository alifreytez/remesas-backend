meta {
  name: Restore User
  type: http
  seq: 11
}

patch {
  url: {{baseUrl}}/users/1/restore
  body: none
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
