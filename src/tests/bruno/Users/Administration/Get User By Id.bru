meta {
  name: Get User By Id
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/users/1
  body: none
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
