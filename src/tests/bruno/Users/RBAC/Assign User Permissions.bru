meta {
  name: Assign User Permissions
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/users/{{testUserId}}/permissions
  body: json
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "permisos": [{{testPermissionId}}]
  }
}

vars:pre-request {
  testUserId: 2
  testPermissionId: 53
}

script:pre-request {
  const baseUrl = bru.getEnvVar('baseUrl') || 'http://127.0.0.1:4000/api/v1';
  bru.setVar('baseUrl', baseUrl);
}
