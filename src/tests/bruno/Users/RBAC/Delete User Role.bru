meta {
  name: Delete User Role
  type: http
  seq: 28
}

delete {
  url: {{baseUrl}}/users/1/roles/1
  body: none
  auth: bearer
}

headers {
  x-client-channel: web
  x-device-id: bruno-test-device
}

auth:bearer {
  token: {{accessToken}}
}
